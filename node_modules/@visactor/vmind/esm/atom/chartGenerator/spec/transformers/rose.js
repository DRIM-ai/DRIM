import { color, commonLabel, data, discreteLegend } from "./common";

export const roseField = context => {
    var _a;
    const {cell: cell, spec: spec} = context;
    return spec.valueField = cell.radius || cell.angle, cell.color && (spec.categoryField = null !== (_a = cell.category) && void 0 !== _a ? _a : cell.color, 
    spec.seriesField = cell.color), spec.outerRadius = .8, spec.innerRadius = .2, {
        spec: spec
    };
};

export const roseAxis = context => {
    const {spec: spec} = context;
    return spec.axes = [ {
        orient: "angle",
        domainLine: {
            visible: !1
        },
        grid: {
            visible: !1,
            alignWithLabel: !1
        },
        label: {
            visible: !0
        }
    }, {
        orient: "radius",
        grid: {
            visible: !1,
            smooth: !0
        }
    } ], {
        spec: spec
    };
};

export const pipelineRose = [ data, color, roseField, roseAxis, discreteLegend, commonLabel ];
//# sourceMappingURL=rose.js.map
